/*
 *  File for specific challenge solving
 */

// use { ... } for function params in order to use object destructuring
// eslint-disable-next-line camelcase
exports.queensAttack = function queensAttack({n, k, r_q, c_q, obstacles}) {
  return null;
};
